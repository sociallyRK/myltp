module RealestatesHelper
  def check_realestates_owner
    # realestate = current_user.financialinstruments.realestates.where(:id => params[:id]).first
    # if realestate.nil?
    #   flash[:error] = "That's not your real esate property."
    #   redirect_to realestate(params[:id])
    # end
  end
end
